import { useMemo, useState } from "react";
import { Badge, DDItem, Dropdown, Meter, Pagination, useToast } from "../../../components/ui";
import { csvDownload, kes } from "../../../lib/format";
import { CASES, FEEDBACK, PIPELINE, RULES, type MonitorRule, type SarCase } from "../data/sarData";
import { AssignCase, CaseDrawer, FilingWizard, RuleModal } from "../modals/SarModals";
import "../styles/sar.css";
export function SarMonitoring({onNavigate}:{signal:{action:string;n:number};onNavigate:(id:string)=>void}){
 const{push}=useToast();const[cases,setCases]=useState(CASES),[rules,setRules]=useState(RULES),[tab,setTab]=useState("All"),[q,setQ]=useState(""),[p,setP]=useState(1),[focus,setFocus]=useState<SarCase|null>(null),[assign,setAssign]=useState<SarCase|null>(null),[filing,setFiling]=useState<SarCase|null>(null),[rule,setRule]=useState<MonitorRule|null>(null);
 const list=useMemo(()=>cases.filter(x=>(tab==="All"||x.stage===tab)&&`${x.id} ${x.user} ${x.rule}`.toLowerCase().includes(q.toLowerCase())),[cases,tab,q]);const page=list.slice((p-1)*7,p*7);
 const tone=(x:string)=>x==="Critical"?"red":x==="High"?"amber":x==="Medium"?"blue":"grey";
 const file=()=>{if(!filing)return;setCases(x=>x.map(c=>c.id===filing.id?{...c,stage:"Filed",status:"Submitted"}:c));push({kind:"success",title:"SAR submitted for approval",body:`${filing.id} is awaiting Compliance Officer and Super Admin sign-off.`});setFiling(null);setFocus(null)};
 return <><div className="pm-section-head sar-top" style={{marginTop:0}}><div><div className="d-flex gap-2 align-items-center mb-1"><span className="pm-eyebrow">Fraud & risk · Page 16</span><span className="pm-live"><span className="pm-dot green pm-pulse"/>Rules engine online</span></div><h2>Transaction monitoring & SAR</h2><p>Investigate suspicious activity, preserve evidence and submit regulator-ready reports through a dual-controlled workflow.</p></div><div className="d-flex gap-2 flex-wrap"><button className="btn btn-outline-secondary btn-sm" onClick={()=>csvDownload("paymo-sar-cases.csv",cases)}><i className="bi bi-download me-1"/>Export cases</button><button className="btn btn-outline-secondary btn-sm" onClick={()=>onNavigate("fraud")}><i className="bi bi-shield-exclamation me-1"/>Fraud dashboard</button><Dropdown width={230} trigger={()=><button className="btn btn-outline-secondary btn-sm"><i className="bi bi-three-dots"/></button>}>{close=><><div className="pm-dd-head">Compliance operations</div><DDItem icon="bi-shield-check" label="AML & sanctions" hint="Screening controls" onClick={()=>{close();onNavigate("aml")}}/><DDItem icon="bi-diagram-3" label="Risk scoring engine" hint="Risk models and overrides" onClick={()=>{close();onNavigate("risk-scoring")}}/><DDItem icon="bi-life-preserver" label="Incident response" hint="Containment playbooks" onClick={()=>{close();onNavigate("incident")}}/></>}</Dropdown><button className="btn btn-primary btn-sm" onClick={()=>setFiling(cases.find(x=>x.stage==="Compliance decision")??cases[0])}><i className="bi bi-file-earmark-arrow-up me-1"/>Prepare SAR</button></div></div>
 <div className="sar-pipeline">{PIPELINE.map(x=><button key={x.s} onClick={()=>{setTab(x.s.includes("review")?"Review":x.s.includes("investigation")?"Investigation":x.s.includes("Filed")?"Filed":"All");setP(1)}}><span className={`sar-pipe-icon ${x.c}`}><i className="bi bi-arrow-right"/></span><strong>{x.n}</strong><div><b>{x.s}</b><small>{x.t} · SLA {x.sla}{x.b?` · ${x.b} breached`:""}</small></div>{x.b>0&&<Badge tone="red">{x.b}</Badge>}</button>)}</div>
 <div className="row g-3"><div className="col-xl-8"><section className="pm-card h-100"><div className="pm-card-head"><div><h3 className="pm-card-title">Active SAR case queue</h3><p className="pm-card-sub">Stage ownership, case urgency and linked financial exposure.</p></div><div className="sar-search"><i className="bi bi-search"/><input value={q} onChange={e=>{setQ(e.target.value);setP(1)}} placeholder="Search case, subject or rule"/></div></div><div className="pm-tabs">{["All","Review","Investigation","Compliance decision","Filed","Dismissed"].map(x=><button className={`pm-tab ${x===tab?"active":""}`} onClick={()=>{setTab(x);setP(1)}} key={x}>{x}{x!=="All"&&<span className="cnt">{cases.filter(c=>c.stage===x).length}</span>}</button>)}</div><div className="pm-table-wrap"><table className="pm-table"><thead><tr><th>Case / subject</th><th>Rule signal</th><th>Exposure</th><th>Stage</th><th>Owner · age</th><th>Priority</th><th/></tr></thead><tbody>{page.map(c=><tr key={c.id} onClick={()=>setFocus(c)}><td><b>{c.id}</b><div className="pm-td-sub">{c.user} · {c.userId}</div></td><td><b>{c.rule.split(" ")[0]}</b><div className="pm-td-sub">{c.rule.slice(8)}</div></td><td className="pm-num">{kes(c.amount)}</td><td><Badge tone={c.stage==="Compliance decision"?"violet":c.stage==="Filed"?"green":c.stage==="Dismissed"?"grey":"blue"}>{c.stage}</Badge></td><td><b>{c.owner}</b><div className="pm-td-sub">{c.age} old</div></td><td><Badge tone={tone(c.priority)}>{c.priority}</Badge></td><td onClick={e=>e.stopPropagation()}><Dropdown width={180} trigger={()=><button className="btn btn-light btn-sm"><i className="bi bi-three-dots-vertical"/></button>}>{close=><><DDItem icon="bi-folder2-open" label="Open case" onClick={()=>{close();setFocus(c)}}/><DDItem icon="bi-person-plus" label="Assign case" onClick={()=>{close();setAssign(c)}}/><DDItem icon="bi-file-earmark-arrow-up" label="Prepare SAR" onClick={()=>{close();setFiling(c)}}/></>}</Dropdown></td></tr>)}</tbody></table></div><Pagination page={p} total={list.length} pageSize={7} onPage={setP}/></section></div><div className="col-xl-4"><section className="pm-card h-100"><div className="pm-card-head"><div><h3 className="pm-card-title">Filing performance</h3><p className="pm-card-sub">Month-to-date control health</p></div><Badge tone="green">On track</Badge></div><div className="sar-stats">{[["SARs filed","3","4 last month","green"],["Amount reported","KES 2.54M","KES 28.4M YTD","red"],["Average time to file","18h","target < 24h","green"],["False positives","0","4 YTD","green"],["Regulatory feedback","1","5 YTD","blue"],["Law enforcement referrals","1","8 YTD","red"]].map(x=><div key={x[0]}><span className={`sar-stat-dot ${x[3]}`}/><div><small>{x[0]}</small><b>{x[1]}</b><em>{x[2]}</em></div></div>)}</div><div className="sar-note"><i className="bi bi-shield-lock"/><span><b>Dual approval enforced</b>Compliance Officer and Super Admin must approve every external submission.</span></div></section></div></div>
 <div className="row g-3 mt-0"><div className="col-xl-8"><section className="pm-card"><div className="pm-card-head"><div><h3 className="pm-card-title">Monitoring rules engine</h3><p className="pm-card-sub">Live scenarios calibrated for suspicious transaction behaviour.</p></div><button className="btn btn-outline-secondary btn-sm" onClick={()=>setRule(rules[0])}>Rule controls</button></div><div className="pm-table-wrap"><table className="pm-table"><thead><tr><th>Rule</th><th>Trigger condition</th><th>Severity</th><th>Hits</th><th>FP rate</th><th>Last trigger</th><th/></tr></thead><tbody>{rules.map(r=><tr key={r.id} onClick={()=>setRule(r)}><td><b>{r.id}</b><div className="pm-td-sub">{r.name}</div></td><td>{r.trigger}</td><td><Badge tone={tone(r.severity)}>{r.severity}</Badge></td><td className="pm-num">{r.hits}</td><td><Meter value={r.fp} tone={r.fp>40?"#f79009":"#12b76a"}/><small className="ms-1">{r.fp}%</small></td><td>{r.last}</td><td><Badge tone={r.active?"green":"grey"}>{r.active?"Active":"Paused"}</Badge></td></tr>)}</tbody></table></div></section></div><div className="col-xl-4"><section className="pm-card h-100"><div className="pm-card-head"><div><h3 className="pm-card-title">Calibration queue</h3><p className="pm-card-sub">Proposed scenario adjustments</p></div><Badge tone="amber">4 reviews</Badge></div><div className="sar-calibration">{[["MON-001","3 TXNs → 4 TXNs","-15% FP","green"],["MON-002","90% in 1h → 85% in 2h","+10% alerts","amber"],["MON-006","80% cash → 75% cash","+8% FP","amber"],["MON-010","10 → 15 txns/hour","-12% FP","green"]].map(x=><button key={x[0]} onClick={()=>setRule(rules.find(r=>r.id===x[0])??rules[0])}><Badge tone="violet">{x[0]}</Badge><b>{x[1]}</b><small className={x[3]}>{x[2]}</small><i className="bi bi-chevron-right"/></button>)}</div></section></div></div>
 <div className="row g-3 mt-0"><div className="col-xl-7"><section className="pm-card"><div className="pm-card-head"><div><h3 className="pm-card-title">Regulatory feedback tracker</h3><p className="pm-card-sub">Authority requests, received dates and closed-loop action.</p></div><button className="btn btn-outline-secondary btn-sm" onClick={()=>onNavigate("compliance-docs")}>Evidence archive</button></div><div className="pm-table-wrap"><table className="pm-table"><thead><tr><th>SAR</th><th>Authority</th><th>Feedback</th><th>Action taken</th></tr></thead><tbody>{FEEDBACK.map(x=><tr key={x.id}><td><b>{x.id}</b><div className="pm-td-sub">Filed {x.filed}</div></td><td>{x.authority}<div className="pm-td-sub">{x.date}</div></td><td><Badge tone={x.tone}>{x.feedback}</Badge></td><td>{x.action}</td></tr>)}</tbody></table></div></section></div><div className="col-xl-5"><section className="pm-card h-100"><div className="pm-card-head"><div><h3 className="pm-card-title">Workflow & retention</h3><p className="pm-card-sub">Policy controls enforced in production.</p></div><button className="btn btn-outline-secondary btn-sm" onClick={()=>onNavigate("sysconfig")}>Configure</button></div><div className="sar-workflow">{[["Auto-assignment","Round-robin"],["Investigation SLA","8 hours"],["Compliance decision SLA","2 hours"],["Filing SLA","24 hours"],["Breach notification","Slack + email"],["Dismissed archive","90 days"],["Filed SAR retention","7 years"]].map(x=><div key={x[0]}><span>{x[0]}</span><b>{x[1]}</b><i className="bi bi-check2-circle"/></div>)}</div></section></div></div>
 <CaseDrawer item={focus} onClose={()=>setFocus(null)} onAssign={x=>{setFocus(null);setAssign(x)}} onFile={x=>{setFocus(null);setFiling(x)}}/><AssignCase item={assign} onClose={()=>setAssign(null)} onSave={name=>{if(assign){setCases(x=>x.map(c=>c.id===assign.id?{...c,owner:name,stage:"Investigation"}:c));push({kind:"success",title:"Investigation assigned",body:`${assign.id} is now owned by ${name}.`});setAssign(null)}}}/><FilingWizard item={filing} onClose={()=>setFiling(null)} onSave={file}/><RuleModal rule={rule} onClose={()=>setRule(null)} onSave={active=>{if(rule){setRules(x=>x.map(r=>r.id===rule.id?{...r,active}:r));push({kind:"success",title:"Rule control saved",body:`${rule.id} is ${active?"active":"paused"} in production.`});setRule(null)}}}/>
 </>}
